// Configuración de Remotion para el render de las animaciones del curso.
import {Config} from '@remotion/cli/config';

Config.setVideoImageFormat('jpeg');   // formato intermedio de los frames
Config.setOverwriteOutput(true);      // sobrescribir renders anteriores
